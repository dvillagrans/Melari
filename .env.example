# Site mode for Melari Spa.
#   preview = safe demo build (noindex, no dead forms, CTAs → /services)
#   live    = production build (indexed, real contact/booking; build FAILS if
#             essential business data is missing — see src/lib/business.ts)
SITE_MODE=preview
